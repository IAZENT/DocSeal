#!/usr/bin/env bash
# Helper script to run DocSeal CLI without installation

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONPATH="${SCRIPT_DIR}/src:${PYTHONPATH}"

python -m docseal.cli.main "$@"
